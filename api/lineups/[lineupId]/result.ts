import type { VercelRequest, VercelResponse } from '@vercel/node';
import { method, parseBody, respondError, tenantContext } from '../../../server/runtime.js';
export default async function handler(req: VercelRequest, res: VercelResponse): Promise<void> { if (!method(req, res, ['POST'])) return; try { const context = await tenantContext(); const lineupId = String(req.query.lineupId ?? ''); const lineup = await context.db.from('floyd_dfs_generated_lineups').select('status').eq('id', lineupId).eq('tenant_id', context.tenantId).single(); if (lineup.error) throw lineup.error; if (lineup.data.status !== 'ENTERED') { res.status(409).json({ error: 'Contest results can only be recorded for an ENTERED lineup.' }); return; } const body = parseBody(req); const result = await context.db.from('floyd_dfs_contest_results').insert({ tenant_id: context.tenantId, generated_lineup_id: lineupId, actual_dk_points: typeof body.actualDkPoints === 'number' ? body.actualDkPoints : null, finish_position: typeof body.finishPosition === 'number' ? body.finishPosition : null, finish_percentile: typeof body.finishPercentile === 'number' ? body.finishPercentile : null, payout: typeof body.payout === 'number' ? body.payout : null, roi: typeof body.roi === 'number' ? body.roi : null, result_payload: body }).select('*').single(); if (result.error) throw result.error; res.status(201).json({ result: result.data }); } catch (error) { respondError(req, res, error); } }
