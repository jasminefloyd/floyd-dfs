import type { VercelRequest, VercelResponse } from '@vercel/node';
import { cors, method, parseBody, respondError, tenantContext } from '../../server/runtime.js';
type Change = { eventType?: string; materiality?: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'; subject?: string; previousState?: unknown; newState?: unknown; source?: unknown };
export default async function handler(req: VercelRequest, res: VercelResponse): Promise<void> { if (!method(req, res, ['POST'])) return; try { const context = await tenantContext(); const body = parseBody(req); const entered = Array.isArray(body.enteredLineups) ? body.enteredLineups as Array<{ id?: string }> : []; const changes = Array.isArray(body.changeEvents) ? body.changeEvents as Change[] : []; if (changes.length) { const saved = await context.db.from('floyd_dfs_change_events').insert(changes.map((event) => ({ tenant_id: context.tenantId, generation_run_id: body.generationRunId ?? null, event_type: event.eventType ?? 'UNKNOWN', subject: String(event.subject ?? 'SLATE'), previous_state: event.previousState ?? {}, new_state: event.newState ?? {}, materiality: event.materiality ?? 'LOW', source: event.source ?? {}, affected_lineup_ids: entered.flatMap((lineup) => lineup.id ? [lineup.id] : []) }))); if (saved.error) throw saved.error; } if (!changes.length) { cors(req, res); res.status(200).json({ decision: { decision: 'KEEP', requiresUserConfirmation: false, immutableLineupIds: entered.flatMap((lineup) => lineup.id ? [lineup.id] : []), affectedStages: [], rationale: 'No material pre-lock changes were detected.' } }); return; } const severity = { LOW: 1, MEDIUM: 2, HIGH: 3, CRITICAL: 4 }; const highest = changes.reduce((best, value) => (severity[value.materiality ?? 'LOW'] > severity[best.materiality ?? 'LOW'] ? value : best), changes[0]); const materiality = highest.materiality ?? 'LOW'; const decision = materiality === 'CRITICAL' ? 'REBUILD' : materiality === 'HIGH' ? 'ADJUST' : 'KEEP'; const affectedStages = /injur|availab|lineup|role/i.test(highest.eventType ?? '') ? ['RESEARCH', 'SPORT_ADJUSTMENT', 'PROJECTION', 'OPTIMIZE', 'SELECTION'] : /projection|model/i.test(highest.eventType ?? '') ? ['PROJECTION', 'OPTIMIZE', 'SELECTION'] : /contest|ownership|duplication/i.test(highest.eventType ?? '') ? ['OPTIMIZE', 'SELECTION'] : ['RESEARCH']; const immutableLineupIds = entered.flatMap((lineup) => lineup.id ? [lineup.id] : []); cors(req, res); res.status(200).json({ decision: { decision, requiresUserConfirmation: immutableLineupIds.length > 0 && decision !== 'KEEP', immutableLineupIds, affectedStages, rationale: immutableLineupIds.length && decision !== 'KEEP' ? `${highest.eventType ?? 'Change'} is ${materiality}, but entered lineups remain immutable. Explicit user confirmation is required before any replacement or adjustment.` : `${highest.eventType ?? 'Change'} is ${materiality}; the earliest affected stages are ${affectedStages.join(', ')}.` } }); } catch (error) { respondError(req, res, error); } }
