import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { AppPage, EmptyState, ErrorBox, Metric, StatusBadge } from '../components/AppPrimitives';
import { formatDate } from '../lib/formatters';
import { floydRequest } from '../lib/floydDfsClient';

type RecordValue = Record<string, unknown>;
export default function RunPage() { const { runId = '' } = useParams(); const [data, setData] = useState<RecordValue | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { void floydRequest<RecordValue>(`/api/runs/${encodeURIComponent(runId)}`).then(setData).catch((reason) => setError(reason instanceof Error ? reason.message : 'Unable to load this run.')); }, [runId]); const run = data?.run as RecordValue | undefined; const stages = Array.isArray(data?.stages) ? data.stages as RecordValue[] : []; const lineups = Array.isArray(data?.lineups) ? data.lineups as RecordValue[] : []; return <AppPage eyebrow="03 / RUN DETAIL" title="Generation run." subtitle="The complete stage lineage behind this decision set.">{error ? <ErrorBox message={error} /> : !data ? <EmptyState text="Loading run details…" /> : <div className="space-y-4"><section className="rounded-xl border border-slate-200 bg-white p-4 shadow-[var(--shadow-subtle)]"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-wide text-slate-500">{String(run?.sport ?? 'DFS')} · {String(run?.contest_format ?? 'contest')}</p><h2 className="mt-1 text-xl font-black text-[#0b1f3a]">{String(run?.id ?? runId)}</h2></div><StatusBadge status={String(run?.state ?? 'unknown')} /></div><div className="mt-4 grid gap-2 sm:grid-cols-3"><Metric label="Created" value={formatDate(String(run?.created_at ?? ''))} /><Metric label="Entries" value={String(run?.requested_entry_count ?? '—')} /><Metric label="Lineups" value={String(lineups.length)} /></div></section><section className="rounded-xl border border-slate-200 bg-white p-4 shadow-[var(--shadow-subtle)]"><h2 className="text-sm font-black uppercase tracking-wide text-[#0b1f3a]">Stage lineage</h2><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{stages.map((stage, index) => <div key={String(stage.id ?? index)} className="rounded-lg border border-slate-200 bg-slate-50 p-3"><div className="flex justify-between gap-2"><span className="text-xs font-black text-[#0b1f3a]">{String(stage.stage ?? 'Stage')}</span><span className="text-[10px] font-bold uppercase text-slate-500">{String(stage.status ?? '—')}</span></div><p className="mt-2 text-[11px] text-slate-500">{String(stage.completed_at ?? stage.created_at ?? 'Timestamp unavailable')}</p></div>)}</div></section><div className="flex flex-wrap gap-3"><Link to={`/research/${runId}`} className="rounded-md bg-[#0b1f3a] px-4 py-2.5 text-xs font-black text-white">View research</Link><Link to="/history" className="rounded-md border border-slate-200 bg-white px-4 py-2.5 text-xs font-black text-slate-700">Back to history</Link></div></div>}</AppPage>; }
