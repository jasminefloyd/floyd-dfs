import type { LineupCandidate, ResearchPackage, SelectionPackage, ValidatedSlate } from './contracts.js';

export interface OpenAiSelectionOptions { apiKey: string; model?: string; endpoint?: string; fetcher?: typeof fetch; }
export async function selectWithOpenAi(input: { slate: ValidatedSlate; research: ResearchPackage; candidates: LineupCandidate[]; selection: SelectionPackage }, options: OpenAiSelectionOptions): Promise<SelectionPackage> {
  const fetcher = options.fetcher ?? fetch;
  const response = await fetcher(options.endpoint ?? 'https://api.openai.com/v1/responses', { method: 'POST', headers: { authorization: `Bearer ${options.apiKey}`, 'content-type': 'application/json' }, body: JSON.stringify({ model: options.model ?? 'gpt-5', store: false, input: `Choose only existing candidate IDs for this DraftKings contest. Never create or modify a lineup. Contest size: ${input.slate.contest.contestSize ?? 'unknown'}. Entries: ${input.slate.contest.userEntryCount}. Candidates: ${JSON.stringify(input.candidates.map((candidate) => ({ id: candidate.id, median: candidate.median, ceiling: candidate.ceiling, tournamentRank: candidate.tournamentRank, leverageScore: candidate.leverageScore, duplicationRisk: candidate.duplicationRisk, playerIds: candidate.playerIds })))}. Return the strongest candidate IDs in order.`, text: { format: { type: 'json_schema', name: 'selection', strict: true, schema: { type: 'object', additionalProperties: false, properties: { candidateIds: { type: 'array', items: { type: 'string' } } }, required: ['candidateIds'] } } } }) }); if (!response.ok) throw new Error(`OpenAI Selection API returned HTTP ${response.status}.`); const payload = await response.json() as Record<string, unknown>; const text = typeof payload.output_text === 'string' ? payload.output_text : extractOutputText(payload.output); if (!text) throw new Error('OpenAI Selection returned no structured output.'); const parsed = JSON.parse(text) as { candidateIds?: unknown[] }; const allowed = new Set(input.candidates.map((candidate) => candidate.id)); const ids = (parsed.candidateIds ?? []).map(String).filter((id) => allowed.has(id)); if (!ids.length) throw new Error('OpenAI Selection returned no valid optimizer candidate IDs.'); const limit = Math.min(input.slate.contest.userEntryCount, input.slate.contest.maxEntriesAllowed ?? input.slate.contest.userEntryCount); const selectedLineups = ids.slice(0, limit).flatMap((id, index) => { const candidate = input.candidates.find((item) => item.id === id); if (!candidate) return []; return [{ candidateId: candidate.id, bulletNumber: index + 1, selectionType: candidate.candidateTypes[0] ?? 'OPENAI_SELECTED', explanation: 'Selected from the optimizer candidate set by the Selection Agent using contest context.', newsContext: input.research.findings.filter((finding) => candidate.playerIds.includes(finding.subjectId) && finding.bucket === 'NEWS_EXTERNAL_CONTEXT').slice(0, 2).map((finding) => `${finding.sourceName}: ${finding.finding}`), rationale: [`Median ${candidate.median.toFixed(1)} with a ${candidate.ceiling.toFixed(1)} ceiling.`, `Optimizer profile: ${candidate.candidateTypes.join(', ') || 'ranked candidate'}.`], playerIds: candidate.playerIds, rosterSlots: candidate.rosterSlots, salaryUsed: candidate.salaryUsed, salaryRemaining: candidate.salaryRemaining, median: candidate.median, ceiling: candidate.ceiling }]; }); return selectedLineups.length ? { ...input.selection, selectedLineups } : input.selection;
}
function extractOutputText(output: unknown): string | undefined { if (!Array.isArray(output)) return undefined; const parts = output.flatMap((item) => item && typeof item === 'object' && Array.isArray((item as Record<string, unknown>).content) ? (item as Record<string, unknown>).content as unknown[] : []); return parts.map((part) => part && typeof part === 'object' && typeof (part as Record<string, unknown>).text === 'string' ? (part as Record<string, unknown>).text : '').join('') || undefined; }
