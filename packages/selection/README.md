# Selection Agent

Selection chooses only from `OptimizerPackage` candidates, limits the portfolio to the user’s configured entry count, includes concise rationale/news context, and blocks when the optimizer candidate set is empty. It never creates or mutates a lineup.
