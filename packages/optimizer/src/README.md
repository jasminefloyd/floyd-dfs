# Optimizer implementations

Constraints, simulation, field model, duplication model, and ranking implementations will be added here.
# Optimize implementation

No AI agent is used in this stage. Strategy weights are supplied through `ObjectiveProfile` configuration.
