# Optimize Service

Deterministic roster constraint enforcement, bounded lineup enumeration, objective profiles, candidate metrics, correlation metadata, field/duplication estimates, and candidate categorization belong here.

The optimizer consumes only validated slate, research, adjustment, and projection packages. It never invents players, bypasses salary/roster rules, or creates a lineup outside the candidate set.
